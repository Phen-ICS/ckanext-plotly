# Pipeline stages
stages:
  - pre-validate
  - validate
  - build
  - publish

# Global variables
variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"
  PYTHONDONTWRITEBYTECODE: "1"
  PYTHONUNBUFFERED: "1"

# Workflow
workflow:
  rules:
    - if: $CI_COMMIT_TAG
      when: always
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: always
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: always
    - when: never

# Pip cache
cache:
  key: "$CI_COMMIT_REF_SLUG"
  paths:
    - .cache/pip

# Default Python image and bootstrap
default:
  image: ${CI_DEPENDENCY_PROXY_GROUP_IMAGE_PREFIX}/python:3.10-alpine
  tags:
    - ics-docker
  before_script:
    - python -m pip install --upgrade pip setuptools wheel

# Version check: block MR if version hasn't been incremented compared to master
version:check:
  stage: pre-validate
  image: ${CI_DEPENDENCY_PROXY_GROUP_IMAGE_PREFIX}/python:3.10-alpine
  before_script:
    - apk add git
    - git fetch origin master
  script:
    - |
      LOCAL_VER=$(grep 'version=' setup.py | head -1 | cut -d'"' -f2)
      REMOTE_VER=$(git show origin/master:setup.py | grep 'version=' | head -1 | cut -d'"' -f2)
      echo "Local version: $LOCAL_VER, Remote (master) version: $REMOTE_VER"
      if [ "$LOCAL_VER" = "$REMOTE_VER" ]; then
        echo "Error: Plotly version must be incremented before merging (current: $LOCAL_VER)";
        exit 1;
      fi
      echo "Version check passed: $LOCAL_VER > $REMOTE_VER"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: always

# Linting (ruff)
lint:ruff:
  stage: validate
  script:
    - pip install ruff==0.16.0
    - ruff check ckanext

# Format check (ruff format)
lint:ruff-format:
  stage: validate
  script:
    - pip install ruff==0.16.0
    - ruff format --check ckanext

# Security scan (bandit)
security:bandit:
  stage: validate
  script:
    - pip install bandit
    - bandit -r ckanext -x ckanext/plotly/tests

# Python dependency audit
security:pip-audit:
  stage: validate
  allow_failure: true
  script:
    - pip install pip-audit
    - pip-audit

# Build Python package
build:package:
  stage: build
  script:
    - pip install build twine
    - python -m build
    - twine check dist/*
  artifacts:
    when: always
    expire_in: 7 days
    paths:
      - dist/

# Publish python package to the public PyPI (pypi.org, manual).
# Requires a PYPI_API_TOKEN CI/CD variable (masked) holding a pypi.org API
# token for this package. Manual (not automatic): unlike the GitLab
# registry, a version can't be re-uploaded once published to pypi.org, so
# this needs a deliberate click rather than running on every push. Gated on
# the default branch rather than a git tag, since this team doesn't use
# tags; `when: manual` is what actually prevents an accidental publish.
publish:pypi:public:
  stage: publish
  image: ${CI_DEPENDENCY_PROXY_GROUP_IMAGE_PREFIX}/python:3.10-slim
  needs:
    - build:package
  variables:
    TWINE_USERNAME: "__token__"
    TWINE_PASSWORD: "$PYPI_API_TOKEN"
  before_script:
    - python -m pip install --upgrade pip setuptools wheel twine
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: manual
    - when: never
  script:
    - test -d dist && ls -l dist
    - twine upload --non-interactive dist/*

# Trigger deploy pipeline in fair3r-docker-alt starting from integration (automatic)
# Launches the full pipeline: ansible_lint → deploy_integration → deploy_validation.
# fair3r-docker-alt fetches this MR's build:package wheel directly via
# needs:project (see its .gitlab-ci.yml fetch:preview:plotly job) — no
# publish/registry step needed on this side.
trigger:fair3r-docker-alt:integration:
  stage: publish
  needs:
    - job: build:package
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: on_success
    - when: never
  variables:
    PLOTLY_PLUGIN_REF: $CI_COMMIT_REF_NAME
    CI_MERGE_REQUEST_IID: $CI_MERGE_REQUEST_IID
  trigger:
    project: ics/fair3r-docker-alt
    branch: main
    strategy: depend
